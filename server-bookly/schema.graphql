type Organisation {
  id: ID
  name: String
  address: String
  email: String
  phone_number: String
}

type Service {
  id: ID
  name: String
  price: Float
}

type Query {
  organisations: [Organisation]
  organisation(id: ID): Organisation

  services: [Service]
  service(id: ID!): Service
}
